language: ruby
rvm:
  - 2.1.0
before_script:
  - cp config/travis_db.yml config/database.yml
  - psql -c 'create database nilgiri_test;' -U postgres
  - bundle exec rake db:test:prepare
script: bundle exec rake spec
